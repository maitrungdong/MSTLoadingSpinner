import React from 'react';
import { MSTLoadingSpinner } from './components/MSTLoadingSpinner';

function App() {
    return (
        <div className="app">
            <MSTLoadingSpinner />
        </div>
    );
}

export default App;
